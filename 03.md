You are continuing the InnaIT WIAM enterprise extension for:
- Device Registry + Device Assignment
- Magic Link first-time bootstrap login
- FIDO-only onboarding

IMPORTANT:
Assume implementation is already done.

Implement only these parts in this prompt:
1. Unit tests
2. Integration tests
3. Security tests

Do not redesign implementation.
Follow existing InnaIT WIAM testing style:
- JUnit 5
- Mockito
- SpringBootTest
- Testcontainers Oracle / Redis / Kafka where needed
- Embedded Kafka where practical
- MockMvc / WebTestClient as appropriate

======================================================================
A. UNIT TESTS
======================================================================

Create unit tests for:
- assignment validation
- status transitions
- device dedicated rules
- enrollment eligibility checks
- reassign/revoke/return logic
- magic link eligibility checks
- magic link single-use enforcement
- bootstrap disable after FIDO activation

Specific expectations:
- dedicated device cannot have more than one active assignment
- assignment across tenant fails
- revoked/retired/lost/damaged device fails validation
- magic link only allowed when:
  - no active FIDO credential
  - bootstrap enabled
  - first login pending
- magic link reuse fails
- bootstrap state disabled after successful FIDO activation

======================================================================
B. INTEGRATION TESTS
======================================================================

Create integration tests for:
1. create user -> create account -> register device inventory -> assign device -> send magic link -> verify magic link -> enroll FIDO -> activate
2. attempt FIDO enrollment without assignment -> fail
3. attempt assignment across tenant -> fail
4. reassign device -> old assignment closed first
5. return device -> status back to IN_STOCK
6. expired magic link -> fail
7. reused magic link -> fail
8. magic link after FIDO activation -> blocked

Verify:
- DB state transitions
- FIDO_CREDENTIALS.DEVICE_ID linkage
- ACCOUNT_BOOTSTRAP_STATE updates
- device and assignment status transitions
- audit records created
- Kafka events emitted

======================================================================
C. SECURITY TESTS
======================================================================

Create security tests for:
- user cannot access another user’s assigned device
- tenant A admin cannot assign tenant B device
- revoked device blocked in enrollment and auth usage
- bootstrap session blocked from non-onboarding APIs
- public magic-link verify endpoint does not leak user existence
- magic link token tampering fails
- expired magic link token fails
- reused magic link token fails

======================================================================
D. TESTING TOOLS AND FIXTURES
======================================================================

Use:
- Testcontainers Oracle for repository/service/integration tests
- Testcontainers Redis for bootstrap/magic-link state if Redis is used
- Embedded Kafka or test Kafka for event verification
- reusable test data builders for tenant/user/account/device/assignment/bootstrap state

Create:
- TestDataFactory for:
  - tenant
  - user
  - account
  - device
  - assignment
  - bootstrap state
  - auth transaction
- helper assertions for:
  - device status
  - assignment status
  - bootstrap state
  - audit event presence
  - emitted Kafka event type

======================================================================
E. OUTPUT FORMAT
======================================================================

Return only:
1. Unit test classes
2. Integration test classes
3. Security test classes
4. Test fixtures/builders/utilities

Do not return implementation code in this prompt.
Follow existing InnaIT WIAM test naming and project style.