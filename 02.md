You are continuing the InnaIT WIAM enterprise extension for:
- Device Registry + Device Assignment
- Magic Link first-time bootstrap login
- FIDO-only onboarding

IMPORTANT:
Assume schema, entities, repositories, services, and validators are already implemented.

Implement only these parts in this prompt:
1. Auth Orchestrator integration
2. Credential Service FIDO integration
3. REST controllers and DTOs
4. Kafka events and audit integration
5. Admin UI API contracts
6. Login portal API flow contracts

Do not implement tests yet.
Do not redesign unrelated modules.

======================================================================
A. AUTH ORCHESTRATOR INTEGRATION
======================================================================

Update Auth Orchestrator.

Add MAGIC_LINK as bootstrap auth method.

When user initiates login:
- if account has no enrolled FIDO credential
- and bootstrap is enabled
- and assigned FIDO device exists
then return:
- primaryMethods = ['MAGIC_LINK']

Add auth states:
- INITIATED
- MAGIC_LINK_SENT
- PRIMARY_VERIFIED
- ONBOARDING_REQUIRED
- FIDO_ENROLLMENT_IN_PROGRESS
- COMPLETED
- FAILED
- ABORTED

Implement APIs:
- POST /api/v1/auth/login/initiate
- POST /api/v1/auth/login/magic-link/send
- GET /api/v1/auth/login/magic-link/verify?token=...
- POST /api/v1/auth/bootstrap/session/validate
- POST /api/v1/auth/bootstrap/session/expire

Behavior:
- on Magic Link verification create restricted bootstrap session
- redirect to onboarding/FIDO enrollment flow
- bootstrap session must not access regular APIs
- after FIDO activation bootstrap is expired

Security rules:
- magic link TTL 5 minutes
- single-use only
- max resend limit configurable
- no user enumeration
- must be tenant-bound
- must be tied to auth transaction / correlation ID

======================================================================
B. CREDENTIAL SERVICE FIDO INTEGRATION
======================================================================

Update existing FIDO enrollment flow.

Methods:
- beginRegistration(accountId, deviceId, displayName)
- completeRegistration(accountId, deviceId, credentialResponse)

Before issuing registration challenge:
- validate device assignment exists
- validate assigned account/user matches accountId
- validate device status is ALLOCATED or PENDING_ACTIVATION
- validate bootstrap or normal session is allowed to perform onboarding
- prevent enrollment using unassigned device

On success:
- update DEVICE_REGISTRY status -> ACTIVE
- update DEVICE_ASSIGNMENTS status -> ACTIVE
- update ACCOUNT_BOOTSTRAP_STATE:
  - FIDO_ENROLLED = 1
  - BOOTSTRAP_ENABLED = 0
  - FIRST_LOGIN_PENDING = 0
- store credential linkage back to device_id

Do NOT add any SoftToken code.

======================================================================
C. REST CONTROLLERS AND DTOS
======================================================================

Create secure REST APIs under:

/api/v1/device-registry/*
/api/v1/device-assignments/*
/api/v1/auth/login/magic-link/*
/api/v1/auth/bootstrap/*

Device inventory:
- POST /api/v1/device-registry
- POST /api/v1/device-registry/bulk-import
- PATCH /api/v1/device-registry/{deviceId}
- GET /api/v1/device-registry/{deviceId}
- GET /api/v1/device-registry
- POST /api/v1/device-registry/{deviceId}/retire
- POST /api/v1/device-registry/{deviceId}/decommission

Device assignment:
- POST /api/v1/device-assignments
- PATCH /api/v1/device-assignments/{assignmentId}/activate
- PATCH /api/v1/device-assignments/{assignmentId}/revoke
- PATCH /api/v1/device-assignments/{assignmentId}/return
- POST /api/v1/device-assignments/{assignmentId}/reassign
- GET /api/v1/device-assignments/user/{userId}
- GET /api/v1/device-assignments/account/{accountId}
- GET /api/v1/device-assignments/available

Delivery tracking:
- POST /api/v1/device-registry/{deviceId}/delivery-events
- GET /api/v1/device-registry/{deviceId}/delivery-events

Enrollment validation APIs:
- POST /api/v1/device-registry/validate-enrollment
- GET /api/v1/device-registry/account/{accountId}/eligible-devices

Permissions:
- SUPER_ADMIN / TENANT_ADMIN / USER_ADMIN can manage inventory and assignment
- HELPDESK may view and update delivery/activation assistance
- End users may only view their assigned devices
- Magic Link verification endpoint is public but token-bound and signed

Use existing ApiResponse envelope and tenant-aware authorization.

Create DTOs for:
- register device
- bulk import devices
- device search/list
- device assignment create/reassign/revoke/return
- delivery log event
- validate enrollment
- magic link send / verify
- bootstrap session validate/expire

======================================================================
D. KAFKA EVENTS AND AUDIT
======================================================================

Publish Kafka events:
- device.registered
- device.updated
- device.assigned
- device.assignment.activated
- device.assignment.revoked
- device.returned
- device.reassigned
- device.retired
- device.decommissioned
- device.enrollment.blocked
- device.activated
- auth.magic_link.sent
- auth.magic_link.verified
- auth.magic_link.expired
- auth.bootstrap.session.created
- auth.bootstrap.disabled

Each event must include:
- tenant_id
- correlation_id
- actor_id
- device_id where relevant
- assignment_id where relevant
- user_id/account_id where relevant
- old/new status where relevant

Persist audit trail for:
- who mapped device to user
- who approved
- who dispatched
- who activated
- who revoked
- reason codes
- magic link sent / verified / expired / blocked

======================================================================
E. ADMIN UI API CONTRACTS
======================================================================

Define API contracts for Angular Admin Console additions:

A. Device Inventory Module
- device list
- import inventory CSV
- filter by status/vendor/model
- device detail
- stock / assigned / active / returned views

B. Device Assignment Module
- assign device to user/account
- view current assignment
- reassign
- revoke
- return to stock
- delivery acknowledgement

C. User Detail / Account Detail integration
- assigned devices tab
- enrollment status
- activation status
- lifecycle history
- bootstrap state

D. End-user self-service
- view my assigned FIDO devices
- view delivery / activation instructions
- start enrollment only for eligible assigned devices

======================================================================
F. LOGIN PORTAL FLOW CONTRACTS
======================================================================

Update Login Portal flow contracts for:
- MAGIC_LINK option for first-time login
- /login/magic-link-sent
- /login/onboarding

Onboarding flow:
1. show assigned device
2. begin FIDO enrollment
3. complete FIDO enrollment
4. confirm success
5. continue to full authenticated session

Return route/API interaction design and response payloads expected by UI.

======================================================================
G. OUTPUT FORMAT
======================================================================

Return only:
1. Auth Orchestrator changes
2. Credential Service FIDO changes
3. Controllers
4. DTOs
5. Event payloads/publish calls
6. Audit integration
7. Admin UI API contracts
8. Login portal API flow contracts

Do not add tests in this prompt.
Follow existing InnaIT WIAM coding and package style.